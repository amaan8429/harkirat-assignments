//this is an type now .
//types and interfaces are not so diffirent . just a small diffirence in syntax . types can't be used in classes and cannot extend . thus interfaces have more features
type PersonInterface3 = {
  name: string;
  age: number;
};
